@page "{id:int?}"
@model AppointmentSchedule.Pages.Appointments.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Appointment.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Appointment.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Appointment.CreatedDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Appointment.CreatedDate)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-warning"asp-page="./Edit" asp-route-id="@Model.Appointment?.ID">Edit</a> 
    <a class="btn btn-secondary" asp-page="./Index">Back to List</a>
</div>
